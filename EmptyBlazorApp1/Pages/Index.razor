@page "/"
@using Microsoft.AspNetCore.Components.Authorization;
@using EmptyBlazorApp1.Entities

@if (_user is User user) {
    <Logined User="@user" />
} else {
    <LoginComponent OnLoginSuccess="(u) => {_user = u; StateHasChanged();}" />
}

@code {
    public User? _user { get; set; }
}